query SearchMediaByTitle($first: Int!, $title: String!) {
  searchMediaByTitle(first: $first, title: $title) {
    nodes {
      ...searchMediaFields
    }
  }
}

fragment searchMediaFields on Media {
  id
  type
  titles {
    canonical
  }
}
